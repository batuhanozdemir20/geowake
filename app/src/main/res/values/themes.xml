<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GeoWake" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:itemBackground">#434343</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorSecondary">#8A8A8A</item>
        <item name="alertDialogTheme">@style/alert_dialog_theme</item>
    </style>

    <style name="Theme.GeoWake" parent="Base.Theme.GeoWake" />
</resources>